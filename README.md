# 1. Install git and git-lfs

### Linux

```
sudo apt-get -y install git git-lfs
git lfs install
```

#### OSX

```
brew install git git-lfs
git lfs install
```

# 2. Clone the repo

```
git clone --depth 1 https://github.com/satorici/blacklists.git
cd blacklists
git lfs pull
```
